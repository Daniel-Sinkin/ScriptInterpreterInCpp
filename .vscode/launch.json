{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "ds_lang_cli (Debug)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/ds_lang_cli",
            "args": ["examples/simple.ds"],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false
        }
    ]
}